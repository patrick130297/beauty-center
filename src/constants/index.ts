export enum LocalStorageKey {
  BOOKINGS = 'bookings',
  LANGUAGE = 'language',
}

export enum Language {
  EN = 'en',
  PT = 'pt',
}

export enum BookingPrefix {
  ID = 'booking-',
}

export enum DefaultValue {
  EMPTY_ARRAY = '[]',
  DEFAULT_LANGUAGE = 'en',
}

export enum Locale {
  EN_US = 'en-US',
  PT_BR = 'pt-BR',
}

